export default function ingredientReducer(state, action) {
  switch (action.type) {
    case ADD_INGREDIENT:
      return()
    case REMOVE_INGREDIENT:
      return()
    default:
      return state
  }
}
